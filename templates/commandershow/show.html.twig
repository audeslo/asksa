{% extends 'commandershow/commandershow.html.twig' %}

{% block content %}

    {#{% block body %}#}
    <div class="col-lg-10">

        <h1>Detail produit</h1>

        <table class="table-bordered table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commandershow.id }}</td>
            </tr>
            <tr>
                <th>commande</th>
                <td>{{ commandershow.commandeshow }}</td>
            </tr>
            <tr>
                <th>Produit</th>
                <td>{{ commandershow.produit }}</td>
            </tr>

            <tr>
                <th>capacite</th>
                <td>{{ commandershow.capacitecartonshow }}</td>
            </tr>

            <tr>
                <th>code bar carton</th>
                <td>{{ commandershow.capacitebidonshow }}</td>
            </tr>


            </tbody>
        </table>

        <div class="row">
            <a class="btn btn-info col-md-3" href="{{ path('commandershow_index') }}">
                <i class="fa fa-chevron-left"></i>
                Retour à la liste
            </a>


            <a class="btn btn-primary col-md-2 offset-6" href="{{ path('commandershow_edit', { 'id': commandershow.id }) }}">
                <i class="fa fa-pencil"></i>
                Modifier
            </a>

        </div>
    </div>
{% endblock %}
