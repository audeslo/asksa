{% extends 'commander/commander.html.twig' %}

{% block content %}

    {#{% block body %}#}
    <div class="col-lg-10">

        <h1>Detail produit</h1>

        <table class="table-bordered table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commander.id }}</td>
            </tr>
            <tr>
                <th>commande</th>
                <td>{{ commander.commande }}</td>
            </tr>
            <tr>
                <th>Produit</th>
                <td>{{ commander.produit }}</td>
            </tr>

           {# <tr>
                <th>capacite</th>
                <td>{{ commander.capacite }}</td>
            </tr>

            <tr>
                <th>code bar carton</th>
                <td>{{ commander.codebarecaton }}</td>
            </tr>

            <tr>
                <th>code bar produit</th>
                <td>{{ commander.codebareproduit }}</td>
            </tr>#}

            <tr>
                <th>Capacite carton</th>
                <td>{{ commander.capacitecarton}}</td>
            </tr>

            </tbody>
        </table>

        <div class="row">
            <a class="btn btn-info col-md-3" href="{{ path('commander_index') }}">
                <i class="fa fa-chevron-left"></i>
                Retour à la liste
            </a>


            <a class="btn btn-primary col-md-2 offset-6" href="{{ path('commander_edit', { 'id': commander.id }) }}">
                <i class="fa fa-pencil"></i>
                Modifier
            </a>

        </div>
    </div>
{% endblock %}
