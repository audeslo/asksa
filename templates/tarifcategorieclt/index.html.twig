{% extends 'tarifcategorieclt/tarifcategorieclt.html.twig' %}

{# page tableau et recherche #}

{% block content %}

    <div class="card mb-3">
        <div class="card-header">
            <h2>
                <i class="fa fa-table" ></i>
                Liste des tarifs des clients</h2></div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-2">
                    <a href="{{ path('tarifcategorieclt_index') }}" class="btn btn-primary ">
                        <i class="fa fa-chevron-right"></i>
                        Nouveau
                    </a>
                </div>

                <div class="offset-8 col-md-2 ">
                    <a href="#" class="btn btn-success ">
                        <i class="fa fa-print"></i>
                        Imprimer
                    </a>
                </div>
            </div>
            <hr>

            <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">

                <thead>
                <tr>

                    <th>Catégories</th>
                    <th>Produits</th>
                    <th>Montant</th>
                    <th>Borne suppérieure</th>
                    <th>Borne inférieure</th>
                    <th>Observation</th>
                    <th></th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                {% for tarifcategorieclt in tarifcategorieclts %}
                    <tr>

                        <td>{{ tarifcategorieclt.categorie }}</td>

                        <td> {{tarifcategorieclt.produit}}</a></td>

                        <td>{{ tarifcategorieclt.montant }}</td>
                        <td> {{tarifcategorieclt.bornesupperieur }}</a></td>

                        <td>{{ tarifcategorieclt.observation }}</td>

                        <td> {{tarifcategorieclt.borneinferieure }}</a></td>

                        <td>
                              {% include('tarifcategorieclt/viewmodal.html.twig') %}
                        </td>
                        <td>
                            <a  class="" href="{{ path('tarifcategorieclt_show', {'id': tarifcategorieclt.id}) }}">
                                <i class="fa fa-edit tm-product-delete-icon"></i>
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <div class="card-footer small text-muted">Hier à 11:59 </div>

{% endblock %}
