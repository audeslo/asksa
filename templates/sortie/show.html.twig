{% extends 'sortie/sortie.html.twig' %}

{% block content %}

    {#{% block body %}#}
    <div class="col-lg-10">

        <h1>Detail produit</h1>

        <table class="table-bordered table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sortie.id }}</td>
            </tr>
            <tr>
                <th>commande</th>
                <td>{{ sortie.vente }}</td>
            </tr>
            <tr>
                <th>Produit</th>
                <td>{{ sortie.produit }}</td>
            </tr>

            <tr>
                <th>capacite</th>
                <td>{{ sortie.capacitebidon }}</td>
            </tr>

            <tr>
                <th>code bar carton</th>
                <td>{{ sortie.quantitecarton }}</td>
            </tr>


            </tbody>
        </table>

        <div class="row">
            <a class="btn btn-default col-md-3" href="{{ path('sortie_index') }}">
                <i class="fa fa-chevron-left"></i>
                Retour à la liste
            </a>


            <a class="btn btn-primary col-md-2 offset-6" href="{{ path('sortie_edit', { 'id': sortie.id }) }}">
                <i class="fa fa-pencil"></i>
                Modifier
            </a>

        </div>
    </div>
{% endblock %}
