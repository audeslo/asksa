{% extends 'sortie/sortie.html.twig' %}

{# page tableau et recherche #}

{% block content %}

    <div class="card mb-3">
        <div class="card-header">
            <h2>
                <i class="fa fa-table" ></i>
                Liste des produits: {{ vente.client}}</h2></div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-2">
                    <a href="{{ path('sortie_new') }}" class="btn btn-primary ">
                        <i class="fa fa-chevron-right"></i>
                      Ajouter
                    </a>
                </div>

                <div class="offset-8 col-md-2 ">
                    <a href="#" class="btn btn-success ">
                        <i class="fa fa-print"></i>
                        Imprimer
                    </a>
                </div>
            </div>
            <hr>

            <table class="table table-bordered" id="dataTable" >

                <thead>
                <tr>

                    {#<th>commande</th>#}
                    <th>Produit</th>
                    <th>Qté achetee</th>
                    <th>Bidon/carton</th>
                    <th>Qté bidon/Litre</th>
                   {# <th>Etat</th>#}
                    <th></th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                {% for sortie in sorties %}
                    <tr>

                       {# <td>{{ commander.id }}</td>
#}
                        <td> {{sortie.produit }}</a></td>

                        <td>{{ sortie.quantiteachete}}</td>

                        <td>{{ sortie.capacitebidon }}</td>

                        <td>{{ sortie.quantitecarton }}</td>


                        {#  <td>
  #}{#                              {% include('commande/viewmodal.html.twig') %}#}{#
                          </td>#}
                        <td>
                            <a  class="" href="{{ path('sortie_show', {'id': sortie.id}) }}">
                                <i class="fa fa-edit tm-product-delete-icon"></i>
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            <div class="row">
                <div class="col-md-2">
                    <a class="btn btn-default col-md-3" href="{{ path('vente_index') }}">
                        <i class="fa fa-chevron-left"></i>
                        Retour à la liste
                    </a>
                </div>
            </div>


        </div>
    </div>
    <hr>




    <div class="card-footer small text-muted">Hier à 11:59 </div>

{% endblock %}
